# 推荐反馈与评测

打开任一岗位详情，选择“符合”或“不符合”。系统把每个岗位最新一次选择作为显式人工判断；该判断只用于评测，不会改变岗位权重或训练模型。

累计至少 50 个不同岗位，并判断当前推荐 Top10 的全部岗位后，运行：

```bash
python -m app.cli.evaluate
```

样本不足或 Top10 尚有未判断岗位时，命令返回退出码 2 和 `collecting_feedback`，不会生成虚假的 Precision@10。设置页会显示收集进度和最终结果。
